<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="3072-update-allow-direct-loan-repayments-description" author="micropay">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">SELECT COUNT(1) FROM c_configuration WHERE name = 'allow-direct-loan-repayments'</sqlCheck>
        </preConditions>
        <update tableName="c_configuration">
            <column name="description" value="When enabled, loan repayments may also be posted directly on the loan (cash, bank, mobile money, etc.) in addition to savings transfer (always allowed). When disabled, only savings transfer repayments are permitted."/>
            <where>name = 'allow-direct-loan-repayments'</where>
        </update>
    </changeSet>

</databaseChangeLog>
